    <item id="{0}"
        name="{1}"
        weight="{2}"
        attack="{3}"
        mattack="{4}"
        defense="{5}"
        range="{6}"
        speed="{7}"
        image="{8}"
    />
    </item>
